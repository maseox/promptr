# Ignorer les modules Node.js (générés par npm install)
node_modules/

# Ignorer les fichiers d'environnement (clés API sensibles)
.env
.env.local
*.env
DEPLOY.md

# Ignorer les logs Winston
logs/
*.log

# Ignorer le build Vite (doit être regénéré sur Render)
dist/

# Ignorer les builds ou caches (optionnel mais recommandé)
.DS_Store
npm-debug.log
yarn-error.log